{
  "name": "sinatra-template",
  "private": true,
  "dependencies": {
    
  },
  "scripts": {
    "dev": "tailwindcss -i ./public/css/tailwind.css -o ./public/css/app.css --watch",
    "build": "tailwindcss -i ./public/css/tailwind.css -o ./public/css/app.css --minify",
    "install-psp-packages": "mkdir -p node_modules/@psp && cp -r ../packages/@psp/* node_modules/@psp/ 2>/dev/null || echo 'Note: Run this from env/ directory after pulling latest packages'"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "daisyui": "^5.0.50",
    "tailwindcss": "^4.1.12"
  }
}
